import React from 'react';

import './contacts.css';

export const Contacts: React.FC = () => {
    return (
        <section>
            <div className='container'>
                <h2>Contacts</h2>
            </div>
        </section>
    );
}